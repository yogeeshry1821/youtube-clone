import React, { useEffect, useState } from 'react'
import Button from './Button'

const ButtonsList = () => {

  return (
      <div className='flex'>
          <Button name ="all"/>
          <Button name ="yogeesh"/>
          <Button name="yogeesh"/>
          <Button name="yogeesh" />
          <Button name="yogeesh" />
          <Button name="yogeesh" />
          <Button name="don" />
          <Button name="don" />
          <Button name="don" />
          <Button name="don" />
          <Button name="don" />
          <Button name="don" />
          <Button name="don" />
    </div>
  )
}

export default ButtonsList